# Typing and native execution evidence — 2026-09-16

Production changes were committed in coherent slices: Akashic `f2dffd5`
(idle terminal service), `de6a6aa` (acknowledged coverage reuse); MegaPad
`ec1794d` (typing retention/backpressure), `4bc24f4` (native dispatch/memory),
`efed68a` (host boundaries/snapshots). No native interval, guest timing,
source-loader mode, checked-in watchdog, or applet implementation was changed.
No compiled Forth cache was used.

All artifact paths below are relative to `local_testing/out/typing-20260916/`.
Each physical artifact contains clean repository/tree bindings, native binary
and font hashes, source image hash, offer JSON, physical screenshots, a JSONL
event trace, outcome, and aggregate-RSS/watchdog supervisor result.

| Artifact | Akashic | MegaPad | Result |
| --- | --- | --- | --- |
| `baseline-skb838w4` | `9e42004` | `eaa4d3b` | Offscreen phase samples; diagnostic only |
| `baseline-d5cta9ku` | `9e42004` | `eaa4d3b` | Offscreen phase + suspended-stack samples; diagnostic only |
| `fixed-_4ac4grq` | `de6a6aa` | `eaa4d3b` | Physical cadence failure: `g` rejected as invalid |
| `fixed-u241g_fl` | `de6a6aa` | `ec1794d` | 19/19 visible; 2.435 s first character |
| `fixed-e4drm9u9` | `de6a6aa` | `4bc24f4` | 19/19 visible; 1.690 s first character |
| `fixed-zaa3cacn` | `de6a6aa` | `4bc24f4` | Observer-enabled host profile; diagnostic only |
| `fixed-w5arf1ig` | `de6a6aa` | `efed68a` | 19/19 visible; 1.413 s first character |
| `fixed-8co6ac8t` | `8e95c0f` | `15613d8` | 19/19 visible; 1.262 s first character |
| `fixed-hfpooc1w` | `8e95c0f` | `15613d8` | First-character exit counters; observer-enabled diagnostic |
| `physical-menus-y1qa8j_u` | `8e95c0f` | `15613d8` | Full physical journey passes; 18 milestones, 21 inputs |
| `fixed-gt5zpk40` | `5bd5ac5` | `fe31e71` | 19/19 visible; 1.121 s isolated, 1.591 s burst median |
| `physical-menus-vjtw2luk` | `5bd5ac5` | `fe31e71` | Full physical journey passes; 18 milestones, 21 inputs |

The before native extension SHA256 is
`a94e93df6ef1d223c47734c248ac2a061645e800b0f6addeb968297b507ccb38`;
the new extension is
`c3c9aa8a817079ec662616382c42f666d40d98adb4859f61f58f5dcdf98c6b8b`.
The measured physical harness SHA256 is
`fed3f634c0a7664f6651017db4c75e8a0a5aa5f4adbacd1802dbdb736779b1d9`.
Its exact original copy remains at `out/typing-20260916/physical_typing.py`;
the tracked runner removes unused inherited menu diagnostics without changing
the executed cadence, compositor or ACK path.

Run a fresh regular typing check from the workspace root (requires local
Unix sockets and the physical X11 display):

```sh
/home/kir/miniconda3/bin/python3 akashic/local_testing/physical_typing_cadence.py run \
  --akashic-root "$PWD/akashic" --megapad-root "$PWD/megapad" \
  --font "$PWD/akashic/assets/fonts/DejaVuSansMono.ttf" \
  --output-parent "$PWD/akashic/local_testing/out/typing-20260916" --label fixed
```

Runs are sequential, bounded by the canonical 900 seconds and 3.5 GiB
aggregate RSS; the child also has a 3.5 GiB address-space bound. No worker pool
or enlarged semantic step budget is used. Input remains blocked until a
complete physically composited frame is ACKed. Cadence scheduling delay and
input-to-visible delay are separate trace measurements.

For native dispatch kernels, run `megapad/bench_native_dispatch.py`, optionally
with `--extension-dir` naming an archived native binary. Each kernel has a
1.5-million-step limit, four sequential trials, and records actual work and
result cells. JSON records are preserved beside this ledger as
`typing-native-before-20260916.json` and `typing-native-after-20260916.json`.
The median excludes the first trial. These are kernels, not physical latency.

Focused validation:

- Akashic idle service: 62 passed in 1.95 s.
- Akashic coverage/bank/instrument/glyph selectors: 352 passed in 17.49 s.
- MegaPad viewer/driver/core: 106 passed in 0.75 s via `make test-sequential`.
- Native executor parity: 96 passed in 1.22 s via `make test-simulator`.
- Stack/native/shared-session/clock: 154 passed in 1.68 s, overlapping the
  native selector, via `make test-simulator`.

The `fixed-zaa3cacn/typing-host.prof` profile begins around the first input
and ends after its physical ACK. Profiling adds substantial observer cost;
its times are not entered into the unprofiled comparison table. It identified
repeated snapshot decoding and OS handoffs as follow-up work. The unprofiled
`fixed-w5arf1ig` result qualifies the resulting change. The final full Desktop acceptance is recorded below.

## Full ordinary physical acceptance

Artifact `physical-menus-5jxqkwdc` binds clean Akashic `a730baa` and MegaPad
`efed68a`, with the new extension hash above. The production Forth is unchanged
from the matched typing runs. The existing canonical `desktop-apt1` journey
with ordinary menu captures passed: 18 visible milestones, 21 scripted inputs,
27 physically composited and acknowledged offers, and two complete CELL
fallback gates. Pad text, Daybook task/date/source interactions, menu
lifecycles, normal app launching, and restoration after menus all passed.
No manual input RPCs entered the journey.

The supervisor recorded exit 0, no stop reason, 154.121389 seconds, and
458,764,288 bytes (437.512 MiB) aggregate peak RSS. The physical sink was
`pygame.display.flip` on X11. The 900-second budget and 3.5 GiB guard were
unchanged. This broader run is functional regression evidence, separate from
the matched typing comparison.

Exact invocation from the workspace root:

```sh
/home/kir/miniconda3/bin/python3 \
  worktree-retirement-archives/2026-09-13-rich-interaction/retired-support/diagnostics/physical_menu_acceptance.py \
  --akashic-root "$PWD/akashic" --megapad-root "$PWD/megapad" \
  --font "$PWD/akashic/assets/fonts/DejaVuSansMono.ttf" \
  --output-parent "$PWD/akashic/local_testing/out/typing-20260916"
```

## Conventional interpreter follow-up

MegaPad `fabef61`, `c14470b`, `01dc03c`, and `15613d8` add static
superinstructions, qualified stack spans, direct reference dispatch at known
unsupported positions, and native EXECUTE of prepared colon targets. The
sequential native/loop/memory/clock/snapshot/session selector passed 462 cases
in 4.38 s. No guest timing, capacity or semantic-boundary changes were made.

The new physical cadence artifact `fixed-8co6ac8t` completes all 19 characters:
1.262 s isolated feedback, 2.531 s burst median and 2.274 s final drain. The
supervisor reports exit 0, 63.313 s and 393.164 MiB peak aggregate RSS.
`physical-menus-y1qa8j_u` also passes the complete ordinary journey: 18
milestones, 21 inputs, 27 post-flip ACKs and both CELL fallback gates. It took
143.037 s and peaked at 439.945 MiB. Both used clean Akashic `8e95c0f` and
MegaPad `15613d8`, the ordinary source composition and unchanged resource
limits. The extension SHA256 is
`49a9386fcdc1747192ce1e385b219bd12514f301c8197ddfb412528be084e0fb`.

`interpreter-kernels-20260916.json` preserves five alternating sequential
before/after process pairs with all raw trials and matching work/results.
Each uses the same current Python host and the benchmark's original per-run
step limit; the before binary is the previous `c3c9aa8a...c6b8b` extension.
Medians use 15 warmed trials. No worker pool or concurrent suite was used.
The added dynamic-call kernel is subject to the same 1.5-million-step limit.

Diagnostic `fixed-hfpooc1w` uses balanced per-boundary cProfile calls and native
exit counters, with its harness retained as `profile_interpreter_typing.py`
in the artifact directory. The cumulative cProfile tree remains inconsistent;
only independent native counters inform the follow-up discussion. Its times
are not physical performance evidence. Full results and the prioritized next
interpreter techniques are in
`docs/rich-terminal/INTERPRETER-PERFORMANCE-20260916.md`.

## Bulk primitives and pointer reads

MegaPad `127112e`, `d2ee98a` and `7a76542` keep ordinary compare/fill/copies
and stack-pointer reads native. `4d2f90d` removes the unnecessary numeric
interface version and compatibility gate; the extension and Python owner
are rebuilt together. `fe31e71` keeps bulk handler allocation/cleanup outside
the scalar loop. The final native-default selector passes 726 execution,
memory, bulk, pointer, loop, clock, quantum, CATCH, snapshot and session checks
in 7.72 s. No Forth source, guest timing, capacity or step limit changed.

`interpreter-bulk-kernels-20260916.json` preserves five alternating sequential
process pairs, with 15 warmed trials per kernel and identical work/results.
Both extension and matching Python boundary hashes are bound. Bulk/pointer
kernels improve 66–104x; existing arithmetic/field kernels are within 2% and
the other existing kernels improve 1.10–1.21x. The separate outlining
comparison is in `interpreter-bulk-outlining-20260916.json`.

`fixed-gt5zpk40` reports 19/19 visible characters, 1.121450 s isolated feedback,
1.590776 s burst median and 1.346633 s last-character drain. Worst scheduling
delay is 232.901 ms, separate from these actual-dispatch-to-ACK times. The
supervisor records exit 0, 60.137368 s and 412,524,544 bytes (393.414 MiB)
aggregate peak RSS. Seven physical compositions took 132.757–171.505 ms.

`physical-menus-vjtw2luk` passes the complete ordinary source-mode journey:
18 milestones, 21 inputs, 26 physical ACKs and both CELL fallback gates.
Its milestone and input sequences equal the previous acceptance run; the
number of intermediate offers is not fixed. It took 127.554929 s and peaked
at 461,336,576 bytes (439.965 MiB), with exit 0, no stop reason and no manual
input RPCs. The sink was `pygame.display.flip` on X11.

Both bind clean MegaPad `fe31e71` and Akashic `5bd5ac5`, unchanged production
Forth, and native extension SHA256
`b6f7c92fcf51b34a7e8cf8ed2cd0536e9bc0741c494b2b936fb71ae3803616e7`.
All runs were sequential under the unchanged 900 s / 3.5 GiB guards, with
available system memory above 8 GiB. `interpreter-bulk-physical-20260916.json`
preserves bindings, supervisors, typing timing events and Desktop input/ACK
evidence. Interpretation and next work are in
`docs/rich-terminal/INTERPRETER-BULK-PERFORMANCE-20260916.md`.

## Guest association and physical compositor follow-up

Clean MegaPad `e98c91e` and Akashic `1c7dd9a` qualify the per-composition
glyph raster reuse and single-document mounted-source scan. The native
extension remains `b6f7c92f...3616e7`; no source cache, ABI version,
scheduler, capacity, or step-limit change is involved.

`fixed-t8g31h2a` completes all 19 physical characters: 0.948124769 s isolated
feedback, 1.5781975205 s burst median, and 1.292141873 s final drain.
The 15.5% isolated reduction is useful; the burst median is essentially
unchanged from `fixed-gt5zpk40`. Physical composition spans
50.305760–61.954919 ms across seven frames. It takes 59.898562 s overall
and peaks at 397,619,200 bytes (379.199 MiB), with exit 0 and no guard stop.

`physical-menus-bjjn1z7v` passes all 18 milestones, 21 interactions,
27 post-flip ACKs, and both CELL fallback gates, with the same milestone
and input sequences as `physical-menus-vjtw2luk`. It records zero manual
input RPCs, `pygame.display.flip` on X11, 139.112550 s, and 464,465,920
bytes (442.949 MiB) peak aggregate RSS. Exit is 0 with no guard stop.

`baseline-4ltwfanq` and `fixed-nvpu593p` are separate observer-enabled
single-character diagnostics. Sampled guest work falls from 35,464,851
to 33,244,707 semantic steps. Association's inclusive samples fall from
3,973,320 to 1,744,982 steps. The fixed diagnostic's native counters freeze
at its stop boundary: 33,239,227 native steps, 365,649,653 ns C++ run time,
and 50,193,090 ns settlement. Inclusive host method timers overlap and are
not additive. Diagnostic elapsed time is not physical latency evidence.

The baseline observer mistakenly re-read cumulative native counters at
shutdown. Discard its raw top-level `native_delta`, `native_profile_delta`,
and `native_exits`; use the correctly stopped sampled groups and timers.
The final observer fixes this bookkeeping defect. A preparatory attempt,
`baseline-aorouqms`, failed on an observer-only field-name error and is
excluded. No production fix was required for either diagnostic issue.

Seven saved actual offers from `fixed-gt5zpk40` replay four alternating
times per compositor implementation, with identical pixels and hit maps
on all 56 compositions. Warm medians improve 2.79–2.99x. The replay driver,
previous compositor, and raw report are in `compositor-replay/` beneath the
artifact parent. Final diagnostic harness/observer and analysis scripts are
copied into `fixed-nvpu593p/`; full raw groups and cProfile files remain in
the diagnostic directories.

MegaPad's sequential compositor/viewer selector passes 114 checks.
Akashic's 38 bounded association execution cases and 61 accompanying
structural checks pass. The additional unchanged documentation check
`test_retained_contract_requires_internal_uidl_projection_now` fails on a
stale acceptance heading and is deferred, as detailed in the report.
The compositor selector also corrects three fixtures proven to fail against
the previous compositor's geometry, font-measurement, and image contracts.

Exact focused commands (run in their respective repositories):

```sh
make test-sequential TEST_PATH='tests/test_rich_terminal_pygame_view.py tests/test_rich_terminal_semantic_pygame.py tests/test_session_viewer.py tests/test_rich_terminal_semantic_viewer_input.py'

/home/kir/miniconda3/bin/python3 -m pytest -q \
  local_testing/test_uidl_mounted_association.py \
  local_testing/test_uidl_collection_snapshot_structure.py \
  local_testing/test_uidl_semantic_collections_structure.py \
  local_testing/test_uidl_retained_architecture.py \
  local_testing/test_rich_terminal_uidl_hybrid_adapter.py
```

The ordinary physical commands were run sequentially from the workspace:

```sh
/home/kir/miniconda3/bin/python3 akashic/local_testing/physical_typing_cadence.py run \
  --akashic-root "$PWD/akashic" --megapad-root "$PWD/megapad" \
  --font "$PWD/akashic/assets/fonts/DejaVuSansMono.ttf" \
  --output-parent "$PWD/akashic/local_testing/out/typing-20260916" --label fixed

/home/kir/miniconda3/bin/python3 \
  worktree-retirement-archives/2026-09-13-rich-interaction/retired-support/diagnostics/physical_menu_acceptance.py \
  --akashic-root "$PWD/akashic" --megapad-root "$PWD/megapad" \
  --font "$PWD/akashic/assets/fonts/DejaVuSansMono.ttf" \
  --output-parent "$PWD/akashic/local_testing/out/typing-20260916"

/home/kir/miniconda3/bin/python3 /tmp/profile_typing_layers.py run \
  --akashic-root "$PWD/akashic" --megapad-root "$PWD/megapad" \
  --font "$PWD/akashic/assets/fonts/DejaVuSansMono.ttf" \
  --output-parent "$PWD/akashic/local_testing/out/typing-20260916" --label fixed
```

For reproduction, the preserved `fixed-nvpu593p/profile_typing_layers.py`
can replace the temporary diagnostic path; keep its observer alongside it.
All physical runs retain the 900 s / 3.5 GiB guards. Resource glances show
more than 8 GiB available RAM. No suites ran concurrently.

`rendering-typing-20260916.json` preserves source/binary/font bindings,
physical timing events and Desktop evidence, replay trials, helper counts,
diagnostic aggregates, raw-profile hashes, and reporting limitations.
Interpretation and priorities are in
`docs/rich-terminal/RENDERING-PERFORMANCE-20260916.md`.

## Residual damage and storage authority follow-up

Akashic `2e0ada0` preserves clean residual rows when the existing exact
non-menu coverage proof is unchanged. Akashic `87abdaa` proves clustered
adapter buffers disjoint through one complete authority query, with exact
individual checks for fragmented layouts. No MegaPad production code,
public ABI, version counter, capacity, scheduler, or step allowance changes
in this pass. All new measurements bind clean MegaPad `9bf21e6` and the
same native extension and font as the preceding series.

Three observer-enabled first-character runs use the same row diagnostic:

| Artifact | Akashic | Sampled steps | Observed rebuilt rows |
| --- | --- | ---: | ---: |
| `baseline-sr2nwhqm` | `409a578` | 32,818,784 | 41 |
| `fixed-1rsqlg1c` | `2e0ada0` | 25,920,673 | 5 |
| `fixed-39y7_4ck` | `87abdaa` | 19,719,018 | 5 |

The combined damage map initially contains rows 0, 1, 3, 39, and 42. The
old scan adds unchanged semantic holes back to damage. The fixed scan
rebuilds just those five rows, reducing residual planning from 8,192,442 to
1,433,687 sampled steps. Inclusive storage-check samples fall from 6,422,848
to 540,702 steps; those overlap phase attribution. Total sampled work falls
39.9%. Native counters freeze at the stop boundary in all three runs;
99.98% of final semantic steps execute natively. Observer timings remain
separate from physical latency evidence.

Unprofiled physical results retain the slow initial trial and the fresh
sequential previous/current comparison:

| Artifact | Sources | Isolated | Burst median | Final drain |
| --- | --- | ---: | ---: | ---: |
| `fixed-gt41yelx` | current | 1.177946 s | 2.166327 s | 1.766553 s |
| `baseline-3ccw15y1` | previous | 1.085325 s | 1.904286 s | 2.731717 s |
| `fixed-5wtujzev` | current repeat | 0.635845 s | 1.123901 s | 1.135585 s |

Every trial accepts and physically displays all 19 characters. Current runs
bind Akashic `87abdaa`; the previous run binds `409a578` from a temporary
`/tmp/akashic-typing-before-20260916` detached checkout, removed after the
comparison. Development stays on `main`. The earlier historical result at
`fixed-t8g31h2a` was 0.948125 / 1.578198 / 1.292142 s and is retained in the
new report too. Shared-host timing varies materially: unchanged composition
ranges from 77–114 ms in the first current trial, 56–93 ms in the fresh
previous trial, and 47–64 ms in the repeat. These single runs do not justify
a precise causal latency percentage or a fluidity claim.

The current repeat records a maximum scheduling delay of 333.709 ms,
additional to the actual-dispatch latency above. Its first response contains
522.288 ms to observe the offer, 50.946 ms for harness evidence/reconstruction,
54.796 ms composition, and 7.815 ms flip/ACK. Overall, it takes 60.667660 s
and peaks at 393,678,848 bytes (375.441 MiB). The first current trial takes
67.189900 s / 398,606,336 bytes; the fresh previous trial takes 65.341006 s /
393,240,576 bytes. All exit 0 without a guard stop.

`physical-menus-shyfa8mg` passes 18 milestones, 21 interactions, 26 post-flip
ACKs, and both complete CELL fallback gates, with the same milestone/input
sequences as `physical-menus-bjjn1z7v`. There are zero manual input RPCs;
the physical sink remains `pygame.display.flip` on X11. It takes
123.010410 s and peaks at 463,360,000 bytes (441.895 MiB), with exit 0 and
no guard stop. This qualifies functionality, not full-journey performance.

Focused commands, run sequentially in Akashic:

```sh
/home/kir/miniconda3/bin/python3 -m pytest -qx \
  local_testing/test_rich_menu_projection_damage.py \
  local_testing/test_rich_projection_reuse.py \
  local_testing/test_rich_terminal_hybrid_screen_producer.py

/home/kir/miniconda3/bin/python3 -m pytest -qx \
  local_testing/test_rich_adapter_storage_proof.py \
  local_testing/test_rich_terminal_uidl_hybrid_adapter.py
```

These pass 143 cases in 5.52 s and 70 in 1.94 s respectively. The prior
unrelated stale contract-heading assertion is not rerun or changed here.
Physical typing and Desktop commands are unchanged from the preceding
section. For the fresh previous-code measurement only:

```sh
git -C akashic worktree add --detach /tmp/akashic-typing-before-20260916 409a578
/home/kir/miniconda3/bin/python3 akashic/local_testing/physical_typing_cadence.py run \
  --akashic-root /tmp/akashic-typing-before-20260916 \
  --megapad-root "$PWD/megapad" \
  --font "$PWD/akashic/assets/fonts/DejaVuSansMono.ttf" \
  --output-parent "$PWD/akashic/local_testing/out/typing-20260916" --label baseline
git -C akashic worktree remove /tmp/akashic-typing-before-20260916
```

The actual removal occurred after the final current-code repeat. The row
diagnostic uses `/tmp/typing-row-diagnostic/profile_typing_layers.py` with
the same `run`, roots, font, output-parent and label arguments. Exact harness
copies and their sibling observer are preserved in each diagnostic artifact.
The final diagnostic also retains `report_residual_storage.py` and
`analyze_layer_profile.py`; full raw groups and cProfile files remain there.

All tests are sequential under the existing 900 s / 3.5 GiB physical guards.
Resource glances during final qualifications stay above 9 GiB available RAM.
`residual-storage-20260916.json` retains every unprofiled trial, diagnostic
aggregates and row samples, raw hashes, bindings, and Desktop evidence.
The report is `docs/rich-terminal/RESIDUAL-STORAGE-PERFORMANCE-20260916.md`.

## Control sorting and fragmented storage follow-up

Akashic `96d770d` replaces repeated heapsorts with natural merges through
the existing bounded delta workspace. Identical captured typing banks need
2,359,909 semantic steps before and 679,384 after, a 71.2% reduction, with
identical complete graph maps. The source banks remain immutable and every
64-bit identity cell, duplicate check, and graph/correlation bijection is
preserved. This is a guest-work comparison, not a host-time ratio.

The first whole-path diagnostic exposed an allocation-sensitive regression:
small and large adapter buffers occupied two distant regions, with a live
UCTX in their gap. The enclosing proof correctly rejected that gap but then
queried every actual span. Akashic `a23e058` splits rejected enclosures by
actual buffer starts and proves each complete subgroup. Two separated groups
need three authority queries rather than fourteen. The worst fragmented
case can cost 2n-1 queries for n distinct starts, more than the old fallback;
tests exercise this bound and actual-overlap rejection.

Observer-enabled diagnostics retain unchanged 8,192-step owner boundaries:

| Artifact | Akashic | Total sampled steps | Delta phase | Storage proof, inclusive |
| --- | --- | ---: | ---: | ---: |
| `baseline-x97wukn3` | `174bb56` | 20,153,259 | 7,405,882 | 557,087 |
| `fixed-5j0hwpim` | `96d770d` | 21,947,354 | 5,726,454 | 4,382,933 |
| `fixed-3d35hutv` | `a23e058` | 18,408,273 | 5,726,452 | 548,898 |

The initial bank observer captured owner cells instead of graph IDs. The
helper replay rejected those IDs; the derived evidence excludes them.
Correlations, phase/stack samples, and frozen native counters remain usable.
Correctly captured banks from `fixed-y5cb8bba` supply the helper comparison.
The corrected observer's source hash is bound separately. The final observer
still sees separated buffer allocations, with different context placement;
controlled tests establish the subgroup behavior independently of this run.

Unprofiled `fixed-6fhz3u7z` accepts and physically displays all 19 characters.
It records 0.732568390 s isolated feedback, 1.222341043 s burst median, and
1.371803937 s last-character drain. These are slower than the preceding
`fixed-5wtujzev` repeat, so this pass establishes no physical latency gain
or fluidity qualification. Composition spans 53.612563–69.546257 ms;
maximum scheduling delay is 178.077977 ms, additional to actual-dispatch
latency. Total duration is 66.838370887 s and peak aggregate RSS is
393,474,048 bytes (375.246 MiB), with exit 0 and no guard stop.

`physical-menus-cph7x09w` passes all 18 milestones, 21 interactions,
27 post-flip ACKs, and both complete CELL fallback gates. Milestone and input
sequences equal `physical-menus-shyfa8mg`. There are zero manual input RPCs;
the sink is `pygame.display.flip` on X11. Duration is 141.295617810 s and
peak aggregate RSS is 463,851,520 bytes (442.363 MiB), with exit 0 and no
guard stop. This qualifies function, not full-journey speed.

Both final physical runs bind clean MegaPad `9bf21e6` / Akashic `a23e058`,
the unchanged native extension SHA256
`b6f7c92fcf51b34a7e8cf8ed2cd0536e9bc0741c494b2b936fb71ae3803616e7`,
and the same font and source mode. No ABI, version, capacity, scheduling,
or step-limit change is involved. Development stayed on the main checkout.

Focused commands, run sequentially in Akashic:

```sh
/home/kir/miniconda3/bin/python3 -m pytest -qx \
  local_testing/test_rich_terminal_control_map.py \
  local_testing/test_rich_terminal_hybrid_screen_producer.py

/home/kir/miniconda3/bin/python3 -m pytest -qx \
  local_testing/test_rich_adapter_storage_proof.py \
  local_testing/test_rich_terminal_uidl_hybrid_adapter.py
```

These pass 117 checks in 12.53 s and 82 in 4.31 s. The latter includes the
`e120d06` test-only extension protecting every allocation gap, forcing both
recursive children, and checking the exact query bound inside a DO loop.
The previously documented unrelated stale-heading assertion was not rerun.

Physical typing and Desktop commands are unchanged from the preceding
sections. The diagnostic used
`/tmp/typing-delta-diagnostic/profile_typing_layers.py` with the same `run`,
roots, font, output-parent, and label arguments. Exact harness and observer
copies are preserved in `fixed-y5cb8bba`, `fixed-5j0hwpim`, and
`fixed-3d35hutv`, verified against their recorded hashes. For reproduction,
replace the temporary harness path with the archived copy and keep the
observer alongside it.

The final diagnostic directory also preserves the previous producer and
adapter sources, helper driver and results, and report aggregator. The
helper and aggregation commands from Akashic are:

```sh
/home/kir/miniconda3/bin/python3 \
  local_testing/out/typing-20260916/fixed-3d35hutv/measure_control_join.py \
  fixed-y5cb8bba

/home/kir/miniconda3/bin/python3 \
  local_testing/out/typing-20260916/fixed-3d35hutv/report_delta_sort.py \
  --typing fixed-6fhz3u7z --desktop physical-menus-cph7x09w \
  --output local_testing/evidence/delta-sort-20260916.json
```

The helper writes a new `/tmp/control-join-20260916.json`; the aggregator
defaults to the preserved sibling helper results. Pass `--helper-results`
to aggregate a new helper run. All execution was sequential with resource
glances above 9 GiB available RAM and unchanged 900 s / 3.5 GiB guards.

`delta-sort-20260916.json` retains bindings, diagnostic aggregates, corrected
captured banks, allocation geometry, exact helper maps, physical timing
events, Desktop evidence, archive hashes, and limitations. Interpretation
and the remaining glyph-traversal costs are in
`docs/rich-terminal/DELTA-SORT-PERFORMANCE-20260916.md`.
