[pytest]
# Disk-image tests can materialize many logically large images. Remove passing
# artifacts immediately while retaining the newest failed session for diagnosis.
tmp_path_retention_policy = failed
tmp_path_retention_count = 1
